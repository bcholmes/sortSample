This project was created to demonstrate a sort problem
with Meteor.  Prior to 0.5.5, the sort instruction, "asc"
showed items in the right order. Once 0.5.5 came out,
the order always seemed to be "desc".
